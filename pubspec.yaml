name: smart_app_mamc
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  animate_do: ^5.1.0
  flutter:
    sdk: flutter
  go_router: ^17.5.0
  google_fonts: ^8.2.1
  video_player: ^2.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
